<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.aplikasikasir">



    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Aplikasikasir">

        <activity android:name=".Login">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".BuatBarang"
            android:label="@string/tittle__buat_barang_baru">
        </activity>
        <activity android:name=".LihatBarang"
            android:label="@string/tittle_activity_lihatbarang">
        </activity>
        <activity android:name=".UpdateBarang"
            android:label="@string/tittle_activity_updatebarang">

        </activity>

            <activity android:name=".MainActivity"
                android:parentActivityName=".Login">


            </activity>


    </application>

</manifest>